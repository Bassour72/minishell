
#include "../include/parsing.h"

void swap_tokens_pos(t_token *list, int current_pos, int target_pos)
{
	//[1][4][2][3][5]
	t_token	*tmp;
	int	diff; 
	

	
	
	tmp = list;
	diff = current_pos - target_pos;
	if (diff > 0)
	{
		while (diff > 0)
		{
			cmd
			diff--;
		}
	}
}